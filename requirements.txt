textblob==0.18.0
nltk==3.8.1
colorama==0.4.6
rich==13.7.0
flask==3.1.1
gunicorn==23.0.0

# NOTE: transformers + torch (the HuggingFace emotion classifier) are
# intentionally NOT installed here. Render's free tier (512MB RAM) gets
# OOM-killed (exit 137) when the model loads, so the deployed/live version
# runs on keyword + TextBlob classification only — the analyzer already
# falls back to this automatically when the transformer can't be loaded.
#
# To use the upgraded transformer-based classifier LOCALLY (more accurate,
# but needs ~1GB+ RAM), install the extra file instead:
#   pip install -r requirements-local.txt